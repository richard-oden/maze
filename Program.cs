using System;
using System.Collections.Generic;
using System.Linq;

namespace Maze
{
    class Program
    {
        static void Main(string[] args)
        {
            // var maze = new Maze(24, 12);
            // var pathBuilder = new PathBuilder(maze);
            // var render = new Render(maze);
            // pathBuilder.Start(true, 2);
            // render.Start(2);
        
            // var testMaze = new Maze(4, 4);
            // testMaze.Cells[0,0] = new Cell{Connections = new HashSet<Direction>() {Direction.South, Direction.East} };
            // testMaze.Cells[0,1] = new Cell{Connections = new HashSet<Direction>() {Direction.North} };
            // testMaze.Cells[1,0] = new Cell{Connections = new HashSet<Direction>() {Direction.North, Direction.West} };

            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 0, Y = 0}, new Coordinate {X = 0, Y = 1}));
            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 0, Y = 1}, new Coordinate {X = 0, Y = 0}));

            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 0, Y = 0}, new Coordinate {X = 1, Y = 0}));
            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 1, Y = 0}, new Coordinate {X = 0, Y = 0}));

            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 0, Y = 0}, new Coordinate {X = 1, Y = 1}));
            // Console.WriteLine(testMaze.DoesConnectionExistBetween(new Coordinate {X = 1, Y = 0}, new Coordinate {X = -1, Y = -1}));
        }
    }
}
